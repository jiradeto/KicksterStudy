machine:
  environment:
    USER_NAME: jiradeto
    USER_EMAIL: jiradet.jd@gmail.com
  ruby:
    version: 2.4.0

dependencies:
  pre:
    - ./bin/setup

test:
  post:
    - bundle exec jekyll build

deployment:
  production:
    branch: master
    commands:
      - ./bin/automated